<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:centerColor="@color/colorDicodingBack"
        android:endColor="@color/colorDicodingBack"
        android:startColor="@color/colorDicodingBack"
        android:type="linear" />
</shape>